{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build All",
            "type": "shell",
            "command": "xmake f -m debug && xmake && xmake f -m release && xmake",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}